% Tests calling a procedure with multiple arguments.
begin
	% Run test
	integer z
    procedure foo (integer x, integer y) begin
		z <= (x + y)
        return
    end

    foo(5, 5)
	
	% Check if success
	if (z = 10)
	then
		put "SUCCESS"
	else
		put "FAILURE"
	end
end
